# Prompt 10 — Suppression de questionnaire

## Demande

Ajouter une icône poubelle sur chaque questionnaire de la liste (`/private`) permettant :
- de supprimer le questionnaire côté frontend (carte retirée de la liste)
- de supprimer toutes les données associées côté serveur (`data/projects/{id}/`)

## Changements

### `app/services/project_manager.py`
- Ajout de `delete_project(project_id: str) -> bool` : supprime le répertoire complet via `shutil.rmtree`.

### `app/api/web.py`
- Ajout de `DELETE /api/projects/{project_id}` : vérifie que le projet appartient à l'utilisateur, appelle `project_manager.delete_project()`, retourne `{"deleted": project_id}`.

### `web/app.js`
- Dans `loadProjects()` : la carte projet contient maintenant un `.project-card-actions` avec le badge statut + bouton 🗑 (`btn-trash`). `onclick` stoppe la propagation et appelle `deleteProject()`.
- Ajout de `deleteProject(projectId, filename)` : demande confirmation via `confirm()`, puis `DELETE /api/projects/{id}`, puis recharge la liste.

### `web/style.css`
- Ajout de `.project-card-actions { display: flex; align-items: center; gap: 0.5rem; }`.
