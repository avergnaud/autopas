# PAS Assistant — Variables d'environnement
# Ce fichier est un exemple. NE JAMAIS commiter le fichier .env.
#
# En production, le fichier .env est généré par Ansible depuis ansible/vars/vault.yml.
# En développement local, copier ce fichier en .env et renseigner les valeurs.

# Clé API Anthropic (Claude)
ANTHROPIC_API_KEY=sk-ant-...

# Secret de l'app registration Azure AD
# tenant_id et client_id (non secrets) sont dans data/config/app.yaml
AZURE_CLIENT_SECRET=...

# Clé de signature des cookies de session
# Générer avec : openssl rand -hex 32
SESSION_SECRET_KEY=...
